syntax = "proto3";

package todo;

service ToDoService {
    rpc CreateToDo (CreateToDo) returns (CreateToDoResponse);
}

message CreateToDoResponse {
    todo_id = 1;
}

message CreateToDo {
    string user_id = 1;
    string message = 2;
}